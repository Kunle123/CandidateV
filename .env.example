# Environment
ENVIRONMENT=development

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=candidatev

# Security
SECRET_KEY=your_secret_key_here  # Generate using: openssl rand -hex 32

# Email Configuration (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_specific_password
EMAILS_FROM_EMAIL=noreply@candidatev.com

# CORS Origins (comma-separated)
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# Railway-specific (when deployed)
# RAILWAY_PUBLIC_POSTGRESQL_URL=postgresql://user:pass@host:port/railway

# API Keys
OPENAI_API_KEY=your_openai_api_key_here
STRIPE_API_KEY=your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret_here

# JWT Configuration
JWT_SECRET=your_secure_jwt_secret_here
JWT_ALGORITHM=HS256
JWT_EXPIRATION=86400

# Database Configuration (Local Development)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,https://candidatev.vercel.app

# Service Ports
API_GATEWAY_PORT=8000
AUTH_SERVICE_PORT=8001
USER_SERVICE_PORT=8002
CV_SERVICE_PORT=8003
AI_SERVICE_PORT=8004
PAYMENT_SERVICE_PORT=8005

# Storage Configuration
STORAGE_TYPE=filesystem  # Options: filesystem, s3
STORAGE_PATH=/app/storage

# Redis Configuration
REDIS_URL=redis://redis_cache:6379/0 